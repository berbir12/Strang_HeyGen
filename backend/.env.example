# Required for video generation
OPENAI_API_KEY=
HEYGEN_API_KEY=

# Optional: restrict who can call the API (extension sends X-API-Key or Authorization: Bearer)
# STRANG_API_KEY=

# Optional: CORS origins (comma-separated). Default "*" allows all.
# CORS_ORIGINS=https://yoursite.com,https://your-landing.vercel.app

# Optional: rate limit (requests per window)
# RATE_LIMIT_REQUESTS=10
# RATE_LIMIT_WINDOW_SEC=3600

# Optional: directory for waitlist.json and jobs.json (default: same dir as main.py)
# WAITLIST_DIR=./data
